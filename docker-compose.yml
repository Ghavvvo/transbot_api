version: '3.3'

services:
  # Backend API (Flask + RAG) - DESHABILITADO
  # api:
  #   build: .
  #   container_name: transbot-api
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   ports:
  #     - "127.0.0.1:5000:5000"
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: '1.0'
  #         memory: 2G
  #       reservations:
  #         cpus: '0.5'
  #         memory: 1G
  #   networks:
  #     - transbot-network

  # Frontend (React + Nginx)
  frontend:
    build: ../transbot-frontend
    container_name: transbot-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:80"
    # depends_on:
    #   - api
    networks:
      - transbot-network

networks:
  transbot-network:
    driver: bridge
